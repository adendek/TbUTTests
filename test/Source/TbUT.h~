#include "gmock/gmock.h"
#include "TbUTITresholdProvider.h"
namespace TbUT
{

class TresholdProviderMock: public ITresholdProvider
{
public:
    MOCK_METHOD0(qux, bool());
    MOCK_METHOD0(norf, bool());
};
}
